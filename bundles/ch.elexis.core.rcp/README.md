# ch.elexis.core.rcp

This bundle is split from `ch.elexis.core` and contains the part that is relevant
to the Elexis RCP client application only. It is deliberately refactored to allow
re-use in the myelexis-server.